%So that one doesnt have to use accent marks as seperate escape characters
\usepackage[T1]{fontenc}

%For hypenation
\usepackage[slovak]{babel}

%For pictures
\usepackage{graphicx}
\graphicspath{ {./images/} }

%For graphs
\usepackage{tikz}
\usetikzlibrary{calc, positioning, fit, arrows}

%For maths
\usepackage{calc}

%For svg
\usepackage[inkscapeformat=png]{svg}
\svgpath{images/}

%For plots
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
